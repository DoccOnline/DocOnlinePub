import React from 'react';
import Navigation from '../Navigation'

const AppointmentInfo = ({
                         }) => {
	return (
		<div>
		<Navigation backArrow={true} />
		What do u need to do to prepare yourself for the visit
		</div>
	)
};

export default AppointmentInfo